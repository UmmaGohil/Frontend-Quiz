import React from 'react';
import Layout from '../template/layout';
import Nav from '../components/Home/Nav';

const home = () => {
  return (
    <Layout>
      <Nav />
    </Layout>
  );
};

export default home;
